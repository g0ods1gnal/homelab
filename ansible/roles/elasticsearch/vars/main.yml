---
elasticsearch_version: "8.11.0"
elasticsearch_cluster_name: "soc-lab-cluster"
elasticsearch_node_name: "elk-server-node"
elasticsearch_network_host: "0.0.0.0"
elasticsearch_http_port: 9200

# Note: elasticsearch_password comes from group_vars/all/vars.yml
# which references vault_elasticsearch_superuser_password from vault.yml
